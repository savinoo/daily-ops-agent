services:
  - type: web
    name: daily-ops-agent
    env: python
    plan: free
    autoDeploy: true
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn daily_ops_agent.api.main:app --host 0.0.0.0 --port $PORT
